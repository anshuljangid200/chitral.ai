# MongoDB Connection String
# Get this from MongoDB Atlas: https://www.mongodb.com/cloud/atlas
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
# Replace <USERNAME>, <PASSWORD>, <CLUSTER>, and <DATABASE> with your actual values
MONGO_URI=mongodb+srv://chitral:aadhya%402003%40%40@login.p5yvdtm.mongodb.net/event-ticketing?retryWrites=true&w=majority

# JWT Secret Key
# Use a strong random string (at least 32 characters)
# Generate one: https://www.random.org/ or use `openssl rand -base64 32`
# Keep this secret and don't commit the real value to git
JWT_SECRET=chitral_event_ticketing_secure_jwt_secret_key_2024_change_in_production

# Frontend URL (for CORS)
# Set this to your frontend's deployed URL or localhost for development
FRONTEND_URL=http://localhost:3000

# Node Environment
# Use 'development' for local testing, 'production' for Vercel
NODE_ENV=development

# Server Port (local development only, not used on Vercel)
PORT=5000

